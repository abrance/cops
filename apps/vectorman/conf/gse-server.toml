# gse-server 期望配置（由 cops 下发，勿在云主机手改）
#
# 鉴权按要求先关闭以跑通链路；启用时改为 true 并预登记 agent 凭据。
listen = "0.0.0.0:7100"
auth_enabled = false
heartbeat_interval_secs = 30
heartbeat_timeout_secs = 90

http_enabled = true
http_listen = "0.0.0.0:7101"
# 同一 HTTP 端口托管前端 dist，SPA 路由回退 index.html
http_web_dir = "web"
