# gse-agent 期望配置（由 cops 下发，勿在云主机手改）
#
# 2026-09-26 迁移：Agent 改连 cloud3 的 gse-server（NodePort 30710）。
# token 在仓库里保持为占位 —— 部署脚本会用运行期密钥（VECTORMAN_AGENT_TOKEN）
# 覆盖本文件的 token 行，凭据不进公开仓库。
server_addr = "vectorman.xiaoyxq.top:30710"
agent_id = "cloud2-agent"
token = "__VECTORMAN_AGENT_TOKEN__"
heartbeat_interval_secs = 30
