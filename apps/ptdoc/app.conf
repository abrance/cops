# 部署脚本读取的应用元数据
APP_NAME=ptdoc

# 主服务容器与健康检查
HEALTH_CONTAINER=ptdoc
HEALTH_TIMEOUT=120
HEALTH_URL=http://127.0.0.1:4173/

# 运行期必需变量：缺失时部署立即失败，避免带着空密钥启动后七牛功能静默失效
# 值来自云主机 /opt/cops/secrets/ptdoc.env
REQUIRED_ENV=PTDOC_DATA_KEY
